<!DOCTYPE html>
<html>
<head>
	<title>My Portfolio</title>
	<style>
  .hori{
    float: left;
    width: 50%;
  }
  .r1{
  	display: none;
  	float: left;
  }
  .r2{
  	display: none;
  	float: left;
  }

  #r1:checked ~ .r1{
  	display: block;
  }

  #r2:checked ~ .r2{
  	display: block;
  }

  
  </style>
  <script type="text/javascript">
  	function sub(event) {
  		if(document.getElementById('username').value==""|document.getElementById('password').value=="")
  			alert("Please fill in your info");
  		else
  			document.forms[0].submit();
  	}
  </script>
</head>
<body style="position:absolute; top:25%; left:25%">
<script type="text/javascript">
	function required(){
		document.getElementById('img').setAttribute('required','');
		document.getElementById('link').removeAttribute('required');
	}
	function resetImg(){
		document.getElementById('img').value="";

		document.getElementById('link').setAttribute('required','');
		document.getElementById('img').removeAttribute('required');
	}
	function signout(){
		document.cookie = "id=;";
		window.location.replace("http://localhost:8080/");	
	}
	function getCookie(cname) {
    var name = cname + "=";
    var ca = document.cookie.split(';');
    for(var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
            c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
            return c.substring(name.length, c.length);
        }
    }
    return "";
	}

	var id = getCookie('id');
	//id = id.substring(3,id.length-1);
	if(id!="")
		alert("Login Successful");
	else
		window.location.replace("http://localhost:8080/");
</script>
<label></label>
<form method="POST" enctype="multipart/form-data">
  <div>
    <label>Name </label>
    <input type="text" id="name" name="name" required>
  </div>
  <br>
  <div>
    <label >Profile picture (optional) </label>
    <input type="file" accept="image/*" name="pp">
  </div>
  <br><br><br>
  <label >You must add one of your works to create a portfolio</label><br><br>
  <label >Name of the project:</label>
  <input type="text" required name="title"><br><br>
  <input type="radio" name="radio" id="r1" value="img" onclick="required()">Screenshot 
  <input type="radio" name="radio" id= "r2" value="link" checked="" onclick="resetImg()">Link to website or code repository
  <br><br>
  <div class="r2"><label  >Link: </label><input type="url" name="link" id="link"></div>
  <div class="r1"><label >Screenshot: </label><input type="file" name="img" id="img" accept="image/*"></div>
  <br><br>
  <input type="submit" value="Create Portfolio">
 </form>


</body>
</html>